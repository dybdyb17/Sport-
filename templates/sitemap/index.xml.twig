<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for route in routes %}
  <url>
    <loc>{{ url(route.name) }}</loc>
    <lastmod>{{ lastmod }}</lastmod>
    <changefreq>{{ route.changefreq }}</changefreq>
    <priority>{{ route.priority }}</priority>
  </url>
{% endfor %}
</urlset>
