{% extends 'admin/base.html.twig' %}

{% block admin_body %}
<a href="{{ path('app_admin_conversations') }}" class="btn btn-ghost btn-sm" style="margin-bottom:16px;">
  <i class="ti ti-arrow-left"></i> Toutes les conversations
</a>

<div class="form-card" style="margin-bottom:20px;">
  <div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:16px;">
    <div>
      <div style="font-family:'Barlow Condensed',sans-serif;font-size:13px;letter-spacing:2px;color:var(--gold);">CONV-{{ conversation.id }}</div>
      <h1 style="font-family:'Barlow Condensed',sans-serif;font-size:26px;font-weight:800;text-transform:uppercase;letter-spacing:1px;color:var(--white);margin:6px 0 4px;">
        {{ conversation.client ? conversation.client.nomComplet : '—' }} ↔ {{ conversation.coach ? conversation.coach.nomComplet : '—' }}
      </h1>
      <div style="color:var(--text-dim);font-size:13px;">
        Conversation ouverte le {{ conversation.createdAt|date('d/m/Y \\à H\\hi') }} · {{ conversation.messages|length }} message(s)
      </div>
    </div>
  </div>
</div>

<div class="form-card" style="padding:0;">
  <div style="padding:24px;max-height:600px;overflow-y:auto;">
    {% if conversation.messages|length == 0 %}
      <p style="color:var(--text-dim);text-align:center;padding:32px 0;">Aucun message dans cette conversation.</p>
    {% else %}
      {% for msg in conversation.messages %}
        {% set isClient = conversation.client and msg.author.id == conversation.client.id %}
        <div style="margin-bottom:20px;padding:14px 18px;background:{{ isClient ? 'rgba(200,169,81,.06)' : 'rgba(46,204,113,.06)' }};border-radius:var(--radius);border-left:3px solid {{ isClient ? 'var(--gold)' : 'var(--green)' }};">
          <div style="display:flex;justify-content:space-between;margin-bottom:6px;">
            <strong style="color:var(--white);font-size:14px;">{{ msg.author.nomComplet }}</strong>
            <span style="font-size:12px;color:var(--text-dim);">{{ msg.createdAt|date('d/m/Y \\à H\\hi') }}</span>
          </div>
          <div style="color:var(--text-soft);font-size:14px;line-height:1.55;white-space:pre-wrap;">{{ msg.content }}</div>
        </div>
      {% endfor %}
    {% endif %}
  </div>
</div>
{% endblock %}
